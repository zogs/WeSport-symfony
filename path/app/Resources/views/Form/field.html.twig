{%  block  file_widget %}
    {% spaceless %}

    {% if image_url is not null %}
        <img src="{{ asset(image_url) }}" alt="avatar" />
    {% endif %}
    {{ block('form_widget') }}

    {% endspaceless %}
{% endblock %}